pyarrow>=14
pandas>=2.0
sqlalchemy>=2.0
pyodbc>=5.0
python-dotenv>=1.0.0
splink>=3.0
unidecode>=1.3
rapidfuzz>=3.0
pytest>=7.0
cologne-phonetics>=2.0.0
tqdm
duckdb>=0.9.0

# ML dependencies for Phase 1: Embedding Infrastructure
sentence-transformers>=2.2.0
torch>=2.0.0
faiss-cpu>=1.7.4  # Use faiss-gpu for GPU-accelerated search if needed
scikit-learn>=1.3.0
numba>=0.57.0

# ML dependencies for Phase 2-3: Training and Model
lightgbm>=4.0.0
joblib>=1.3.0

# Pattern Discovery Analysis Module
kmodes>=0.12.2          # K-modes clustering for categorical data
openai>=1.0.0           # DeepSeek API (OpenAI-compatible)
matplotlib>=3.7.0       # Visualization
seaborn>=0.12.0         # Statistical visualization

# Optional: GPU inference dependencies
# Option 1 - Windows (DirectML) or Linux (CUDA):
#   pip install onnxruntime-gpu onnxmltools
# Option 2 - Linux only (faster, requires conda):
#   conda install -c rapidsai -c conda-forge cuml cupy
# onnxruntime-gpu>=1.16.0  # ONNX Runtime with GPU support (Windows DirectML / Linux CUDA)
# onnxmltools>=1.11.0  # Convert LightGBM to ONNX format
