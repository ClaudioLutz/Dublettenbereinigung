# Workflow Status Template

# This tracks progress through BMM methodology Analysis, Planning, and Solutioning phases.
# Implementation phase is tracked separately in sprint-status.yaml

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met (e.g., if_has_ui)
#
# Completion Status:
#   - {file-path}: File created/found (e.g., "docs/product-brief.md")
#   - skipped: Optional/conditional workflow that was skipped

generated: "2026-01-09"
project: "dubletten"
project_type: "data-pipeline"
selected_track: "bmad-method"
field_type: "brownfield"
workflow_path: "_bmad/bmm/workflows/workflow-status/paths/method-brownfield.yaml"

workflow_status:
  - phase: 0
    name: "Documentation"
    status: "skipped"
    note: "Project has adequate existing documentation (architecture.md, index.md, etc.)"

  - phase: 1
    name: "Analysis (Optional)"
    workflows:
      - id: "brainstorm-project"
        status: "_bmad-output/analysis/brainstorming-session-2026-01-09.md"
        completed: "2026-01-09"
        agent: "analyst"
        command: "/bmad:core:workflows:brainstorming"

      - id: "research"
        status: "docs/research/pattern-discovery-research-20260107.md"
        completed: "2026-01-07"
        agent: "analyst"
        command: "/bmad:bmm:workflows:research"

      - id: "product-brief"
        status: "skipped"
        note: "Optional - not created"

  - phase: 2
    name: "Planning"
    workflows:
      - id: "prd"
        status: "required"
        agent: "pm"
        command: "/bmad:bmm:workflows:create-prd"
        note: "Missing - needed to formalize requirements from brainstorming session"

      - id: "create-ux-design"
        status: "skipped"
        note: "Not applicable for data pipeline project"

  - phase: 3
    name: "Solutioning"
    workflows:
      - id: "create-architecture"
        status: "docs/architecture.md"
        completed: "2026-01-06"
        agent: "architect"
        command: "/bmad:bmm:workflows:create-architecture"

      - id: "create-epics-and-stories"
        status: "required"
        agent: "pm"
        command: "/bmad:bmm:workflows:create-epics-and-stories"
        note: "Stories exist (21+ in docs/stories/) but need formal epic structure with brainstorming insights"

      - id: "test-design"
        status: "optional"
        agent: "tea"
        command: "/bmad:bmm:workflows:testarch-test-design"

      - id: "implementation-readiness"
        status: "required"
        agent: "architect"
        command: "/bmad:bmm:workflows:check-implementation-readiness"
        note: "Gate check needed to validate PRD + Architecture + Epics before full implementation"

  - phase: 4
    name: "Implementation"
    workflows:
      - id: "sprint-planning"
        status: "required"
        agent: "sm"
        command: "/bmad:bmm:workflows:sprint-planning"
        note: "Create sprint-status.yaml to track implementation of epics/stories"

# CURRENT STATE SUMMARY:
# ======================
# You have completed brainstorming and research, and have an architecture document.
# Your brainstorming session identified a clear implementation roadmap for the
# deduplication pipeline (4-step structure with pattern classification).
#
# RECOMMENDED NEXT STEPS:
# 1. Create PRD to formalize requirements from brainstorming insights
# 2. Create Epics & Stories to structure the implementation roadmap
# 3. Run implementation-readiness check
# 4. Create sprint plan and begin implementation
#
# ALTERNATIVE (Fast Track):
# If you want to implement immediately from the brainstorming session:
# - Use /bmad:bmm:workflows:create-tech-spec to create detailed implementation spec
# - Use /bmad:bmm:workflows:quick-dev to implement directly
