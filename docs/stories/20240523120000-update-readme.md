## Summary
Updated `README.md` to accurately reflect the current project structure, configuration, and feature set, including the modular `dedupe` package and legacy script locations.

## Context / Problem
The previous `README.md` contained outdated information referencing non-existent directories (`dedupe_splink/`) and old scripts as primary entry points. It did not document new features like address-aware prefiltering or Swisstopo normalization, and listed incorrect environment variable prefixes.

## What Changed
*   **Project Structure**: Updated to show `dedupe/` as core, `scripts/` as entry points, and `legacy/` for old code.
*   **Configuration**: Corrected environment variable prefix to `DEDUPE_DB_`.
*   **Key Features**: Added "Address-Aware Prefiltering" and "Swisstopo Normalization".
*   **Usage**: Updated `run_dedupe.py` arguments list (e.g., `--blocking-mode`, `--swisstopo-db`).
*   **Legacy**: Moved Splink integration details to a "Legacy & Alternative Methods" section.

## How to Test
1.  Read the `README.md` file.
2.  Verify that the directory structure matches the actual codebase (`ls -R`).
3.  Verify that `scripts/run_dedupe.py --help` matches the documented arguments.
4.  Verify that environment variables in `dedupe/config.py` match the documentation.

## Risk / Rollback Notes
*   **Risk**: Low. Documentation update only.
*   **Rollback**: Revert changes to `README.md`.
